{
  "name": "@gridgo/h5",
  "version": "0.1.0",
  "private": true,
  "main": "src/main.tsx",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:h5": "vite build",
    "build:mp-weixin": "echo 'mp-weixin build: 暂未启用，需要时再装 Taro' && exit 1",
    "build:mp-alipay": "echo 'mp-alipay build: 暂未启用' && exit 1",
    "build:mp-toutiao": "echo 'mp-toutiao build: 暂未启用' && exit 1",
    "build:rn": "echo 'rn build: 暂未启用' && exit 1",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "lint": "eslint src --ext .ts,.tsx",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@gridgo/api": "workspace:*",
    "@gridgo/store": "workspace:*",
    "@gridgo/types": "workspace:*",
    "@gridgo/ui": "workspace:*",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "zustand": "^4.5.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "@vitejs/plugin-react": "^4.3.0",
    "happy-dom": "^14.12.0",
    "typescript": "^5.4.5",
    "unocss": "^0.61.0",
    "vite": "^5.3.0",
    "vitest": "^1.6.0"
  }
}
